{
  "name": "qse-analysis-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check app.js && node --check analysis-engine.js"
  },
  "engines": { "node": ">=20" }
}
